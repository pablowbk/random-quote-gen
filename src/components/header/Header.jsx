const Header = ({getRandomQuote}) => {
  return (
    <header className="App-header">
      <p>Random Quotes</p>
      {/* <button onClick={getRandomQuote}>get quote</button> */}
    </header>
  )
}

export default Header;